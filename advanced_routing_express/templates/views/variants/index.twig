{% extends 'layouts/base.twig' %}

{% block main_content %}
    <h2>All Variants</h2>
    <ul>
        {% for variant in variants %}
            <nav>
                {{variant.title}} {{"Price:"}}{{variant.price}} - 
                <a href="/variants/{{ variant.id }}">Edit</a>
                <a href="/variants/{{ variant.id }}/delete">Delete</a>
            </nav>
        {% endfor %}
    </ul>
    <a href="/variants/new">Create Variant</a>
{% endblock %}